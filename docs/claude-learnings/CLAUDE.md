# docs/claude-learnings

When adding, removing, or renaming docs in this directory, update `README.md` to keep the guided index in sync.

Each entry follows the conditional format:
```
**If <task context>:**
→ `filename.md` — <brief description of contents>
```
